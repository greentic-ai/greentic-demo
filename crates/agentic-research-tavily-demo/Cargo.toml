[package]
name = "agentic-research-tavily-demo"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Greentic demo: an Agentic Worker (dw.agent) node running inside a messaging flow, replying in webchat."

[lib]
path = "src/lib.rs"
