# Runtime port for the HTTP server
PORT=8080

# Secrets + pack runtime configuration
PACKS_DIR=./packs
SECRETS_BACKEND=env
PACK_SOURCE=fs
PACK_INDEX_URL=./examples/index.json
PACK_CACHE_DIR=.packs
PACK_REFRESH_INTERVAL=30s
TENANT_RESOLVER=host

# Optional: provide a verification key when using signed packs
# PACK_PUBLIC_KEY=ed25519:BASE64KEY
